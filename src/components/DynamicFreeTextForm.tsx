import React from "react";

type Props = {
  isContent: boolean;
  setIsContent: React.Dispatch<React.SetStateAction<boolean>>;
};

const DynamicFreeTextForm = (props: Props) => {
  return <div>DynamicFreeTextForm</div>;
};

export default DynamicFreeTextForm;
