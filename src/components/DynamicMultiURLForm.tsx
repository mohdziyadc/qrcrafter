import React from "react";

type Props = {
  isContent: boolean;
  setIsContent: React.Dispatch<React.SetStateAction<boolean>>;
};

const DynamicMultiURLForm = (props: Props) => {
  return <div>DynamicMultiURLForm</div>;
};

export default DynamicMultiURLForm;
