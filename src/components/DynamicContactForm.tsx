import React from "react";

type Props = {
  isContent: boolean;
  setIsContent: React.Dispatch<React.SetStateAction<boolean>>;
};

const DynamicContactForm = (props: Props) => {
  return <div>DynamicContactForm</div>;
};

export default DynamicContactForm;
